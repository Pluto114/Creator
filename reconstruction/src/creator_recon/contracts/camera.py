"""Reserved CameraRecord, CoordinateConvention and ImageTransformChain DTOs.

Preserve explicit camera axes, pixel-center mappings, world-to-camera poses and
scale provenance. Unknown calibration is never replaced with invented values.
"""
