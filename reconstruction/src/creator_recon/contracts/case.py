"""Reserved case import, CaseManifest, ordered FrameRecord and RegionSelection DTOs.

Preserve EXIF-normalized original pixels and immutable input identity. Camera
and coordinate contracts belong to camera.py; no complete wire schema exists yet.
"""
