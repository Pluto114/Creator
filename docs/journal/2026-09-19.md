# 2026-09-19：五张图都同意，也可能一起同意错了

从`951c62d`继续，还是在`D:/Creator-newage`。今天没有回去调旧阈值，而是把9月17日留下的正经问题做成代码：一张图里有好几条亮边时，先都留着，让相机和其他视图一起判断。

先冻了12种情况，开发和评价各6种。里面有干净斜杆、真杆加乱纹、只在三张图可见的杆、稳定的杆内亮条、两条都合理的线，以及空场景里一条跨视图稳定的背景线。RGB、相机和粗guide放输入目录；物理轴和“这里到底有没有目标”单独放评价真值。推理跑完前没读答案。

代码现在会从每张图保留多条受支持的图像线，枚举三视图组合形成三维线，再回所有视图查支持。另有分离候选拿到接近票数时，它返回歧义。以前那种“先在每张图拍板，再假装大家说的是同一根杆”的路子终于被拆开了。

结果一半让人高兴，一半挺扎心。真杆旁边每张图都有不同位置的杂纹时，它能找到正确杆；两条真实一致的线同时出现时，也知道自己不该乱选。但一致的杆内亮条离物理轴7.5厘米，五张图全都支持，算法照样信了。空场景中的一致背景线也拿到五票，被当成假物体。说白了，多视图能证明“这些像素可以由同一条三维线解释”，证明不了“这条线就是用户要的杆轴”。

把门槛从三张抬到四张，能挡住每张图乱跳的空背景条纹；代价是把只在三张图可见、误差只有1.3毫米的真杆也拒了。今天没有选一个看起来数字更漂亮的赢家。三视图太容易凑巧，四视图又会误杀遮挡情况，正确设计应该先判断哪些视图可观测，再把支持数当证据，而不是一刀切。

算法方向因此定下来：多候选关联层保留，它负责排不一致和暴露歧义；最终接受前再加物理身份层，先测试多视图外轮廓/宽度和表面随视角变化的响应。如果一条背景细线在所有输入中真的和目标无法区分，就老实返回歧义或让用户补对应，不编一个自动答案。

完整数字和图见[实验报告](../experiments/2026-09-19-multiview-candidate-association.md)，决定见[ADR 0006](../decisions/0006-multiview-consistency-is-not-identity.md)。今天的12例从现在起只能做回归；下一轮要换新的Blender布局，不能拿看过的评价组继续刷分。

收尾检查还抓到一个边界问题：第一次运行的推理入口虽然没使用答案字段，却为了核对ID读取了包含生成答案的完整协议。数值没因此改变，但这不配叫严格隔离。旧运行`rod-multiview-candidates-v1-20260919`原样保留；入口改为只读独立方法配置和输入清单，再用`rod-multiview-candidates-v1-20260919r1`完整重跑。正式报告只引用r1，不能因为“代码里没取那个字段”就假装文件从没被打开。

严格隔离重跑与旧运行的24条评分逐项相同。r1协议SHA256是`4145dcefb400ac1266ff4b48efcb46956831b493ad0fd762edd0d735138d6e77`，推理SHA256是`269acb7ed411de8d949798eed5f71a3ea7a446f4c21c15408d1ebec012dd9688`。145项本地诊断全部通过，Ruff、119份Python源码骨架检查以及核心4项测试/6个子测试通过。

## 还差多远

按最初四个Gate算，G0已经到了收口阶段：数据、坐标、真值隔离、强基线失败和关键反例都比较扎实，但还要用新的Blender布局确认今天的候选/身份设计，才适合正式进入G1。之后还缺第一版有限杆补丁、不同对象上的可重复收益、强基线与消融、真实照片、插件导入/比较/撤回，以及最终报告和复现包。

如果保持每周约30–35小时，达到“欧洲高校能认真看、工程和实验都说得过去”的版本，估计还需6–8周；要做到我愿意称为卓越——独立对象和真实照片结果稳定、失败边界清楚、插件流程完整、报告像一篇小型研究论文——更现实是9–11周，也就是11月底到12月上旬。最大的风险不是写代码速度，而是物理身份层能否在新对象上带来可重复收益；如果两到三周内仍没有这个信号，应及时收窄主张或转成可靠拒绝/评测贡献，不能靠堆界面拖时间。

## 下午加速：终于把新 Blender 身份包跑实了

上午的解析包告诉我们“五张图一致也能一起错”，下午就不再拿同一批人造灰条打转，直接写了新的程序化Blender生成器。8种布局、每种5个环绕视角：干净杆、杆面亮条、相邻双杆、中段遮挡、斜杆、强高光、目标加背景杆，以及最要命的“用户目标为空，但ROI里有另一根真实杆”。每帧同步保存RGB、精确相机、evaluated mesh，再用独立射线求相机Z深度、沿射线距离、表面ID和目标可见掩码。输入目录只有RGB、相机和guide，答案继续关在`data/eval_gt`。

生成器第一遍就踩了Blender枚举显示名的坑，第二遍又把3×3和4×4直接乘了。这俩错都发生在首帧前，旧目录保留，分别换运行号继续。更值得记的是`r2`：40张图都生成成功，多视图却8例全拒绝。往下扒才发现拟合器明明支持4×4外参，重投影函数却只支持3×4，而且外层容错把shape异常吃掉了。要是把这种“全拒绝”包装成保守算法就太扯了。修完后补了4×4回归测试，用`r3`整包重跑，终于得到真的算法结果。

`r3`里，干净、遮挡、斜杆和“目标旁边另有背景杆”都正确，p95轴误差约0.37–1.91毫米；杆面亮条、强高光和相邻双杆都保持歧义，没有强行挑真值最近者。空目标那例仍把另一根真实杆当成目标。这个失败其实很值钱：它把“有一根物理线”和“是用户指定的那根”彻底分开了。

我原本想用“更宽候选包住窄亮带”做身份报警，结果新包里它没改变任何判定。亮条案例本来就多解，干净案例又只零星触发。这个想法先降回诊断量，不为了显得今天有新算法就硬塞主流程。

接着做了一个更朴素的粗guide身份门：三维线回投后，至少3个视角且75%视角要落在用户guide附近，容差取原搜索半宽的四分之一。它在已经看过的20例上做开发回放：解析三视图的空目标假物体从3个降到1个，四视图从2个降到1个；新Blender包从1个降到0；所有原本正确接受都保留。坏消息也写死了：离guide很近的表面错误轴仍然通过。guide只能说“用户大概指这儿”，不能说“这就是物理中心”。

40帧又单独跑了一遍哈希、相机、ID、深度和投影轴检查。35个有目标视角、每个101个轴采样点，全部命中目标或声明的遮挡物；目标mask与`surface_id == 1`逐像素一致。图和完整数字见[新报告](../experiments/2026-09-19-blender-identity-and-guide-guard.md)，设计决定见[ADR 0007](../decisions/0007-guide-is-target-identity-input.md)。失败的初始、`r1`、`r2`和正式`r3`都留在D盘，没有拿覆盖旧结果换整洁。

## 现在到哪了、还差多久

G0现在可以收口：数据链、坐标、真值隔离、候选关联和身份反例都已经用新渲染布局实测，不再只是旧场景上的猜测。这不等于修复算法成功，而是终于有资格进入G1：冻结新的独立对象与guide扰动保留集，验证身份门，再恢复有限端点、保护真实缺口并输出可撤回补丁。

今天大约提前了一个小里程碑。按每周30–35小时，做到欧洲高校会认真看、实验与工程都完整的版本，当前估计还需5–7周；做到卓越水平，还需8–10周。最危险的地方仍是杆面亮条和近邻物体的轴身份，其次是独立对象/真实照片能否复现收益。未来两周要盯算法和保留集，不拿插件界面充进度。

收尾时全仓Ruff、124份Python源码骨架检查、核心4项测试/6个子测试和153项诊断全部通过；三个Python分发包与Blender插件也重新构建成功。GitHub双平台CI等推送后再记最终状态。

## 继续推进：先把“看着像对”这件事拆开

这次接着往前做，没有重装环境，也没有碰旧项目目录。先读交接和上一轮代码，结果又发现两个不能糊弄过去的地方：之前guide其实由三维轴投影而来，不是真人随手画的；墙上虽然有Brick节点，坐标方向却用错了，所谓复杂砖墙并没有真的进入那批图。旧数据留着，旧报告加补充说明，这些成绩只能在原来的条件下解释。

新包换成真正能看到砖纹的背景，10种情况、每种5张图，提示线直接写成屏幕坐标，再向右偏0、8、16像素。最细新杆的半径是0.02，不是把最早0.01那根偷偷换个说法宣称修好了。仍然用精确相机，仍然是同一个圆柱族，还不是跨对象、普通照片的成绩。

有限杆段终于和多候选关联接上了。这里最容易手滑的是：先拟合一条线，再把最上面和最下面一连，看起来完整得很，缺口也被顺手抹掉。现在只让被选中候选对应的原始像素行投正票；没看见就先空着，不擅自猜中间有杆。相机不动，至少4视图才能做留一视图检查。它会重新拟合这些被选中的图像线，所以关联结果和有限段结果分别留档，不能混成一个“真轴”。

还顺手堵了一个挺阴的坑：搜到预算上限时，只剩一个暂时最好的候选，不代表它真的唯一。现在没搜完就返回不确定，坏相机直接报管线错误，不再混进普通的算法拒绝。源码、方法和输入的哈希也成为硬检查，避免冻了配置却继续换代码。

渲染中途连续退出两次，r2拿到的系统退出码是3221226505，没有Python异常栈，原因还没查明。改成每个场景独立Blender进程后就跑完了，但我不把这个现象硬说成查明了显存泄漏。r3用于确认进程隔离，补完“案例和50帧必须全齐”的契约后另开r4。前面失败的东西都在D盘留着。

边跑边记一个不太舒服但有用的现象：干净杆的guide不偏时歧义，偏8px却能输出；真缺口那例三个位置都拒绝。背景一复杂，前面的关联就开始卡住。这个时候如果只报“缺口没被错误填上”就很鸡贼——什么都不输出，当然不填错，但也没帮用户恢复。今天会同时给恢复量和错误量，不能只挑好听的一半。

## r4跑完：别急着庆功，瓶颈比想的更靠前

30次跑了约20分钟，真正正确输出只有s01和s02在8px偏移下的两次。两段都接近完整真杆，误差在毫米量级；但默认0px反而都歧义，谈不上稳。高光例虽然输出了三段，真值一比才发现轴也歪了，p95接近3厘米，还有约0.390米错误新增。空目标旁边的近杆更直接：0px下被错当目标，输出了2.188米。真缺口和遮挡都全拒，恢复量是零。这些都进正式表，不往附录里藏。

我又试着推翻自己的解释：是不是guide外的杂线太多，把guide过滤提前就行？只读审计的答案是否定的。干净例2条竞争线都在guide内，缺口例3条都在，遮挡例11条都在。简单挪一下过滤顺序救不了它们。更扎眼的是30次都至少有一张图碰到8候选上限，提示一移动，留下来的候选排名就可能变。所以接下来先拆开窗口移动和身份提示两件事，查截断敏感性，再验证左右边/宽度的跨视角物理约束。今天不继续调一个“更容易接受”的阈值。

数据这边是实在往前走了：50帧、51,200次独立射线对照，ID零不一致，Z最大差约3.17微米，投影最大差约0.000027像素。但这些是几何约定检查，不是RGB亚像素抗锯齿证明。旧失败和结果都保留，源码/配置/输入/真值分别冻结，新输出没有混进模型输入。

检查合计171项本地诊断通过，相关30项还在NumPy2环境跑过；核心4项/6个子测试、Ruff、128份Python源码边界检查通过。一次无隔离构建因为工具环境没有hatchling失败，随后按既有隔离构建流程完成三个包和插件，推理环境没改。图已经看过，完整数字见[压力测试报告](../experiments/2026-09-19-identity-stress-and-finite-segments.md)。

现在是G1开发中，还没过G1。有限段代码接通了，但独立对象上的重复收益、普通估计相机、强基线比较和正式补丁都还欠着。继续按每周30–35小时算：完整申请展示版5–7周，更扎实的研究/工程版本8–10周。今天补了重要实现，也排除了一个便宜但无效的改法；目前没有理由再把日期往前拍。
