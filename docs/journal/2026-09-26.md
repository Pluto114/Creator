# 2026-09-26 开发日志

今天把三条线并行跑完：同条件的多种子目标证据实验、旧DA3结果回放、截面模型诊断。相机拟合在四种杆观测之间共用，100次拟合覆盖400条几何条件，约230秒完成。省下的是重复计算，没有少留失败记录。

## 今天终于分清了什么

上一轮只能说“结果稳定也可能错”。这次进一步问：哪一种新证据，能挡住哪一种错？20个种子里，每个种子的背景、初始相机和端点噪声保持一致，分别给正确对应、单图交换端点、所有图一致认错旁边杆、所有图一致认错深度四种输入。方法和2px预算先冻结，之后才生成、推理和评分。

只检查端点跨图是否对得上，20/20个单图交换都挡住了，20/20个正常对应保留。但两个“大家一起认错”的机制也都20/20通过。于是再给两个目标点：指向真正目标时，横向和深度错误各20/20被挡；若目标点也跟着错候选走，它们仍各20/20通过。就这么直白：输入一起认错，几何不能凭空知道你到底想要哪根。

别被整齐的20/20骗了。这里的目标点是计算出来的，跨图端点身份也是给定的，没有实现从照片自动找端点或识别目标。更关键的是，正常对应虽然全部通过，原控制三维p95仍在4.14–42.36mm之间，25mm容差下平均恢复率只有79.07%。检查通过只证明与这些像素相容，连“正确对应”都不能消掉共享相机偏差。

三套目标声明、四种检查组合一共4800个决策，但只有20个场景随机种子，不是4800个独立对象。五个相机条件全部留着，每个种子只用完整控制相机求一次Sim3。被拒绝、未决或扣留的正例，最终空输出照记R=0、P未定义；没有删掉它们刷平均数。

## 拿已有模型结果试了一下，没新增收益

旧7组Blender RGB上的真实DA3输出共70行，旧流程有51个候选可继续审查。两个旧点击早已用于选择目标，现在拿它们再做有限段投影检查，51个全保留：双向p95都不超过25mm的27个保住了，但至少一个方向超标的24个也一个没挡住。原16个身份不接受和3个相机扣留照旧。

这不是新身份验证，只是把同一份证据换了个问法。幸好把这步真的跑了，否则很容易把多加一个检查函数，当成可靠性又升了一档。原杆几何、相机和旧评分完全没改；也没有把旧点击假装成未用过的保留集。这7组不是7件实拍独立物体。

## 读取器：原始点里有信号，但不能直接拿来发证书

另一条线新冻了64个解析条件，比较椭圆、双圆和截线三个模型，两种点表示、两fold，总计768行拟合记录。完整椭圆和完整双杆，保留体素格内原始位置后确实更容易在残差上区分；只用体素中心会丢掉一部分信息。

坏消息也够具体：半边椭圆还是容易被双圆解释，平面薄条能被很扁的椭圆解释，圆杆也常被更灵活的双圆拟合得更好。所以这轮只有诊断，没有新自动出轴规则。双圆14次未收敛、椭圆69次和双圆27次碰参数边界，全都留在结果里。

独立复核还补出一个容易说过头的地方：这里不仅PCA轴是共享的，中心、轴向分位边界、半径搜索范围和格权重也用了全体点；raw点分fold后还可能占同一体素。因此只是共享预处理下的截面参数留出，不能称完整独立验证。下一版要按格分组留间隔、只从训练点建立坐标架和权重，且在拟合前锁定规则。

## 按结果收紧下一步，而不是继续堆门槛

算法职责分开：端点检查负责发现跨图矛盾；目标证据负责约束“我要哪根”；三维精度和共享相机偏差另算。旧点击不能重复算新证据，低残差也不能包办这三件事。

下一批优先补真正未参与拟合和选择的RGB观测，固定每条观测的图像来源、用途和获取时间；同时检查它能否暴露相机偏差，而不只验证重投影。共同读取器保留格内位置，先做训练独立的截面预测与复杂度、物理重叠、部分可见性控制，再决定能否恢复轴。新条件先冻、后看分，今天这批从此作为开发材料。

接着才是接回完整基础/候选补丁、合格共同评分和未参与设计的新对象。最早那根0.01细杆还不能据此宣布修好了。本轮没有新增DA3预测或发布自动补丁，旧输入、结果和失败都留在D盘，真值继续只进评价侧。

## 检查与交付

420项全量诊断通过（70.805秒）；本轮42项新增专项在NumPy1.26与2两个已有环境通过。核心边界另有16项测试、15个子检查通过。Ruff与实验包离线构建通过，结果图已实际查看。第一次误用了不带NumPy的静态工具环境运行核心pytest，收集失败；换回已有重建环境后通过，没有重装环境或改测试绕过。

独立审计在正式评价之前保存319个文件收据，评价后全部不变，100份相机结果、400行几何和4800个决策逐项核对。1900个没有输出的决策全部R=0、P=null；匿名输入/真值重生成、配对噪声和一次控制相机对齐复算通过。旧70行回放的评分连接也经另一条只读复核确认。

G1仍未通过。按每周35小时以上的投入，仍预留约1–2周集中推进，这是条件预算，不因为今天计算快就把独立对象、共同读取和相机误差的工作删掉。9月27日是检查点。完整申请项目还差实拍、跨对象重复收益、复现材料和研究写作；“欧洲高校可接受并且卓越”没有一条跑完测试就保证达到的分数线。先把G1的证据做实，再据外部验证重估全项目工期。

[成对目标证据](../experiments/2026-09-26-paired-target-evidence.md) · [旧点击回放](../experiments/2026-09-26-target-projection-replay.md) · [截面诊断](../experiments/2026-09-26-section-model-diagnostic.md) · [ADR0019](../decisions/0019-evidence-roles-and-section-model-diagnostics.md)

收尾来源检查覆盖16个冻结运行、598条源码引用、96份不同冻结源和99份Git核验源。17份今日新源均LF且Git字节一致；历史8份CRLF映射和失败相机首版59份快照完整保留。最终源码边界覆盖241份Python源；11份新JSON严格解析和文档链接检查通过，公开报告仍逐项对应冻结结果。[完整源码审计](../experiments/results/2026-09-26-evidence-git-freeze-audit.json)随代码、报告和图一起提交；缓存、数据及模型仍留D盘。

## 晚续：今天有时间，把新视图真正接进来

这一轮不再停在解析像素上。给已有完整杆、断杆、细杆三个布局各补两个视角，新增6张640×480 Blender RGB；网格、材质、背景板和灯光保留，视角和高度改变。它们是新图，不是新独立物体，更不是实拍。深度、距离、ID、可见掩码和精确相机照常隔离导出；6160条独立Blender射线检查零ID冲突，最大投影差约0.000026px。

我先逐张看新RGB，在可见杆的135行记录两个成员点，再核查这一行的像素值来确定坐标。没有看新相机或杆投影才下点，也没用GT投影。六个点连原图SHA一起封存；±0.5px只是坐标格，不装成已经做过用户误点标定。

旧同高度/变高度各三组数据，五个固定相机条件，共30份旧地图全部原样保留。新视图只靠背景SIFT与地图定位：同一个新feature要有至少两个旧视图一致认出同一track，冲突全排除；旧train且新图y≥192才进PnP，旧validation且新图y<128只评分。旧地图、焦距假设和杆都不动，点击也没进定位。代码里最需要提醒自己的就是这句：新照片是新证据，给新照片定位的地图却还是旧的。

60次PnP和60个两视图杆检查约8.04秒完成，没重跑DA3或旧BA。60个新相机全通过背景验证，p95为0.443–1.110px；评价侧固定旧control的Sim3后，相机中心仍偏1.80–19.72mm。51个旧eligible杆全保留，27个双向p95≤25mm，24个仍超差；9个原拒绝照旧。新图没白拍，它把“只要再来两个视角就能验真”的捷径也检验掉了，但本轮确实没有新增修复收益。

## 继续追问：是新图不够，还是相机把误差吃掉了

看到正常流程仍全保留后，另冻一份事后诊断，只在评价侧替换新相机K或位姿。同一杆、同一旧control对齐、同一点击和2px规则，全都不改；真相机绝不流回正常输入。60杆×4组、120投影槽×4组全部保留。

原51个eligible的支持/未决/矛盾依次为：估计相机51/0/0；只换真K是35/16/0；只换真位姿24/22/5；两者都换31/18/2。只换K居然把16个原25mm读数达标的结果变成未决，而24个超差仍全通过，提醒我们K和位姿的误差可能相互补偿，单换一个部件不能简单解释为改好了。

真K+真位姿下，24个超差里8个还通过、14个未决、2个矛盾；27个达标里23个通过、4个未决。相机更准有帮助，却不是把全杆精度交给两个局部点击验收的理由。局部可见杆成员点不保证端点、长度或缺口正确；2px与25mm也不是同一项指标。这些是特权诊断，不是我们已实现的自动方法，更不能拿来宣称识别成功率。

## 共同读取：把上一轮验证隔离的欠账补掉

新的截面实验按固定原生三维体素分组，训练和验证之间预留guard；128fold都零共享格，最小实际z间隔约80mm。训练中心、PCA、搜索半径和格权只看训练点。改变或删除留出点，不会改变训练坐标架或拟合参数，这点有实际测试。

仍用旧64开发条件，768个模型记录一个没删。完整椭圆和双杆的方向性残差信号仍各8/8；半边宽椭圆却只有2/8更偏向椭圆，圆和薄片也继续让排名误导人。没有新增自动出轴规则。划分同时改变了子集、guard和搜索域，不能把新旧分数差说成单因素提升。

这里也有个工程坑：首版某项测试读取了本机被Git忽略的旧输入，干净CI拿不到。首版完整产物和源码快照保留，修成从已跟踪协议在内存生成夹具后另冻r1。两版64份几何记录逐字节相同、768评分相同；它是可复现性修订，不算又获得一批独立实验。

## 今天收尾与接下来要交付什么

最终462项回归通过（66.579秒），本轮42项新增专项均在NumPy1.26和2通过；核心16测试/15子检查、Ruff、252份源码边界和离线构建通过。独立外审在评价前实际保存421份收据，评价后全部未变，60相机/60杆/120槽、物理读数连接和旧control对齐复算通过；两图已查看并独立核对全部51个可画候选，重叠点没有被删。正常空输出仍R=0、P=null。

最终源码审计覆盖21个活跃run、883条引用、112份不同冻结源、118个Git文件；18份本轮新源字节一致。历史8个换行映射、59份相机失败快照、6份截面首版快照全部保留。已有环境继续复用，模型、缓存、数据和输出留D盘；没有扩插件界面。

下一批优先冻结明确新增的采集标定信息与沿杆分布的端点/缺口观测，让相机定位和整根有限结构都得到约束，再回到真实基础/补丁共同评分。标定板若给已知尺寸或坐标，必须公开声明为正常输入和采集条件，不能把评价GT改名塞进去。截面排名暂时继续只作诊断。

最早0.01细杆的稳定修复还没完成，G1也还没过。这次多做的是新图、新验证链路、隔离修正和更明确的失败定位；不是靠多跑几百行就能缩短剩余工期。G1暂仍按约1–2周集中推进作条件预算；完整申请成果还差独立对象、实拍、复现说明和研究写作，等下一轮物理收益再重新估计。

[新视图验证](../experiments/2026-09-26-heldout-rgb-views.md) · [相机替换诊断](../experiments/2026-09-26-heldout-view-oracle.md) · [截面训练隔离](../experiments/2026-09-26-section-model-trainonly.md) · [ADR0020](../decisions/0020-heldout-views-inherit-map-bias.md) · [源码审计](../experiments/results/2026-09-26-heldout-git-freeze-audit.json)
