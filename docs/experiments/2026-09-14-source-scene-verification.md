# 原场景核对与下一阶段：2026-09-14

用户提供的有效文件为 `C:/Users/123/AppData/Local/Temp/43760_autosave.blend`。已做不可变内容备份：`D:/Creator-newage/data/source_scenes/thin-rod-pilot/43760_autosave-9e64f2feac75.blend`。没有覆盖或保存修改到原文件。

## 已完成的核对

- 场景可在本机 Blender 5.2.1 LTS 正常打开，包含杆、相机、地面、背景、灯光和程序化砖块材质；没有需要补齐的外部贴图。
- 保存时为砖块背景、最细档、父对象 `Empty.001` 的 Z 旋转 30°。没有动画关键帧；实验的其他条件已通过参数重现和原图逐像素核对恢复。
- 固定相机父对象 Z 旋转为 −30°、−15°、0°、15°、30°，将所有圆柱 X/Y 缩放设为 0.01、0.05、0.12，分别接通或断开墙和地面的 Brick Texture → Base Color 连接，可以重现六组条件。
- 原先被称为“直径”的三档数值，对应的实际几何直径为 **0.02、0.10、0.24 场景单位**。保存场景为 Metric、scale_length=1。今后以实际尺寸记录，旧目录及运行产物保留作为历史来源。
- 30 张重渲染与原始 RGBA 比较，**28 张完全一致**。另两张为黑白厚杆 −30°、砖块厚杆 −30°：完全一致的像素比例分别为 99.9866%、99.9267%，最大通道差为 5/255，均值差分别约 0.000114、0.000316/255。微小差异的来源尚未进一步定位，不宣称这两张完全相同。
- 全部相机投影和 Blender 自带 world_to_camera_view 对照，最大偏差约 0.000114 像素。导出分别声明像素边界原点和像素中心索引原点的 K，避免混淆半个像素的约定。
- `Cylinder.003` 与 `Cylinder.002` 的世界顶点集合完全重复，已将前者标为后者的重复几何；保留原场景渲染状态，不在计分时将同一杆算两次。

## 已保存的产物

- 来源、对象/材质清单、重渲染脚本和核对图：`D:/Creator-newage/.runtime/scene-inspection/20260914T071326Z/`
- 各组真实杆中心线端点、尺寸、重复映射，以及五个相机的内外参：`D:/Creator-newage/data/eval_gt/thin-pack-v1/geometry_cameras.json`
- 逐图匹配检查：同目录 `render_verification.json`
- 核对汇总：同目录 `verification_summary.json`

几何与相机记录中，每个视角都有原图哈希及完全匹配标志。两张未完全匹配的原图在正式定量使用前仍需确认；也可使用已保存的重渲染图与同步导出的参数组成新的严格配对版本。旧版先导输入和预测不覆盖。

**尚未完成：**真实逐像素深度、各杆可见掩码、正式几何误差指标，以及任何细杆修复结果。相机和几何文件仅用于隔离评测；不能悄悄作为修复算法输入。专门提供真实相机的诊断实验需单独标记。

## 下一步顺序

1. **完善带答案的数据包。** 同步输出 RGB、相机、真实深度、杆 ID/可见性及真实缺口标记。先检查坐标和深度含义；确保抽样处于细杆上的像素与深度对应，避免抗锯齿产生错误标签。
2. **量化现有模型的错误。** 对比原始深度、全部反投影点和不同置信过滤结果。分别看杆的位置偏差、可观测杆段缺失及真实间隙被错误连接；先验证指标，不预设提升比例。
3. **分离相机误差与杆几何误差。** 正常估计相机的结果作为主线，真实相机仅作单列诊断。再比较更高分辨率、同 ROI 局部裁剪和简单直线拟合，判断值得自研的部分。
4. **实现最小候选。** 从多视图局部图像观测恢复三维直杆中心线，要求跨视图支持；证据不足不强连，保护输入里本来存在的缺口。用前述基线比较实际收益与错误补全。

此阶段现有场景文件和图片足够，不需要用户再拍照、找外部模型或手工输出相机参数。另两种背景/尺寸之外的独立物体可在当前方法出现可重复改善后加入，用于检验泛化。
