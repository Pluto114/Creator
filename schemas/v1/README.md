# 文件协议 v1 设计位置

规范见 [数据契约](../../docs/architecture/02-data-contracts.md)。

这里尚无已发布的运行时 JSON Schema。完整 Pydantic DTO 实现后，从核心自动导出模式并提交黄金样例；不要手写不完整 JSON 文件却将其标记为稳定 v1。Python 包版本 `0.1.0.dev0` 仅标识本次骨架，不表示协议已经实现。
