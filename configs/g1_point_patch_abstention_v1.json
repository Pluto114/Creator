{
  "schema_version": "1.0.0",
  "run_id": "g1-point-patch-abstention-v1-20260924",
  "source_run_id": "rod-evidence-dense-v1-20260916",
  "scope": "Seven unchanged historical complete base/candidate pairs with fixed component-abstention reader; developmental applicability only. Distinguish resolved/partial/unresolved and retain empty positive failures.",
  "variant": "paired_full",
  "voxel_camera_span_fractions": [
    0.0025,
    0.005
  ],
  "region_minimum_views": 3,
  "parallel_workers": 3,
  "readout": {},
  "evaluation": {
    "gt_bundle": "thin-pack-v2-20260914r1",
    "target_rod_ids": [
      5,
      6
    ],
    "tolerances_m": [
      0.02,
      0.05,
      0.1
    ],
    "spacing_m": 0.005
  },
  "freeze_note": "Keep every original valid depth pixel in the materialized base. No confidence pruning, no new fitting, no GT choice of accepted targets. Both complete base and base plus all historically accepted segments use the same pre-existing RGB guide union and fixed readout scales derived from base camera span. Suppression empty in this replay; deletion verified separately with analytic source-ID tests.",
  "engineering_revision": "Only replace whole-component fallback following valid but unsupported splitting with explicit unresolved abstention. Preserve frozen closed thresholds, ROI, camera scale, all base points, original patches and metrics. Check proposals at split depth limit without exceeding it; unresolved components emit no axis."
}
