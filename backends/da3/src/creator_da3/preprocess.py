"""Future exact EXIF-normalized image to DA3 input transform recording.

Record executed resize/crop/pad and pixel-center rules; do not infer them from
an approximate size ratio. The implementation belongs to the pinned adapter.
"""
