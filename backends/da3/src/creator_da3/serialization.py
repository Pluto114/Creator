"""Future BackendPredictionManifest publication with portable numeric arrays.

Write native conventions and preprocessing explicitly, publish a manifest last,
and never serialize torch objects or Python pickle across environments.
"""
